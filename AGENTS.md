# Instructions for AI Agents

## Version Bumping (REQUIRED)

Before EVERY commit, you MUST:
1. Increment the patch version in `VERSION` file (e.g., 0.1.0 → 0.1.1)
2. Update the version in `README.md` to match
3. Include both file changes in the commit

For major/minor bumps, user will explicitly request.
